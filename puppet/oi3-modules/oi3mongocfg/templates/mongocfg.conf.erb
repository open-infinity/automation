systemLog:
    destination: file
    path: "/opt/openinfinity/log/mongodb/mongocfg.log"
    quiet: true
    logAppend: true

storage:
    dbPath: "/opt/openinfinity/data/mongocfg"
    journal:
        enabled: true
    smallFiles: <%= mongo_storage_smallFiles %>

security:
    authorization: <%= mongo_security_authorization %>

processManagement:
    fork: true
    pidFilePath: /var/run/mongodb/mongocfg.pid

net:
#    bindIp: 0.0.0.0
    port: <%= mongocfg_port %>
    
<% if mongo_cluster_type == 'sharded' then %>
sharding:
    clusterRole: configsvr
<% end %>

