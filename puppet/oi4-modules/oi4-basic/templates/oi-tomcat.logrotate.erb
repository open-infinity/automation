/opt/openinfinity/<%= @_toasversion %>/tomcat/logs/catalina.out { 
        copytruncate
        daily
        rotate 14
        compress
        missingok
        size 5M
}

/opt/openinfinity/<%= @_toasversion %>/tomcat/logs/catalina.log {
        copytruncate
        daily
        rotate 14
        compress
        missingok
        size 5M
}

/opt/openinfinity/<%= @_toasversion %>/tomcat/logs/host-manager.log {
        copytruncate
        daily
        rotate 14
        compress
        missingok
        size 5M
}

/opt/openinfinity/<%= @_toasversion %>/tomcat/logs/localhost.log {
        copytruncate
        daily
        rotate 14
        compress
        missingok
        size 5M
}

/opt/openinfinity/<%= @_toasversion %>/tomcat/logs/manager.log {
        copytruncate
        daily
        rotate 14
        compress
        missingok
        size 5M
}

/opt/openinfinity/<%= @_toasversion %>/tomcat/logs/localhost_access_log.txt {
        copytruncate
        daily
        rotate 14
        compress
        missingok
        size 5M
}
