class io4idp {
	exec { "Selfsigned certificate for Idp":
		path => "/usr/bin:/bin",
		command => "/etc/puppet/modules/io4idp/files/create_certs.sh to.be.configured /opt/shibboleth-idp password",
		creates => "/opt/shibboleth-idp/credentials/idp.jks",
	}
}
